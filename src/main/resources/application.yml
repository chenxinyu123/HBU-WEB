#Redis服务器地址
#spring.redis.host: 127.0.0.1
#Redis服务器连接端口 使用默认端口6379可以省略配置
#spring.redis.port: 6379
#Redisf服务器连接密码（默认为空）
#spring.redis.password:
#连接池最大连接数（如果配置<=0,则没有限制）
#spring.redis.jedis.pool.max-active: 8
#spring.redis.database: 4


spring.datasource.driverClassName: com.mysql.jdbc.Driver
spring.datasource.url: jdbc:mysql://127.0.0.1:3306/test?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8
spring.datasource.username: root
spring.datasource.password: cxy981126
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
  jpa:
    show-sql: true
server:
  port: 8888